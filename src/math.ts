export function add(...params) {
  return params.reduce((x, y) => x + y, 0);
}

function multiplier(x) {
  return x * x;
}

export const names = ["haid", "abrar", "zubair", "aliya", "blah"];
export const hadi = "Moalana Hadi";
